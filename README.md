# AI Study-to-Work Coach

Vue3 CDN + SFC + UnoCSS demo for USAII Global AI Hackathon 2026.

```powershell
node serve.mjs
```

Open `http://127.0.0.1:4197/`.

Generated JSON from the V core is read from `src/data/*.json` when available;
`src/data.js` provides the committed fallback demo.
